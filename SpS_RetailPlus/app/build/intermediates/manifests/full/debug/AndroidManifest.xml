<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.controlefantasias"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="23" />

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:configChanges="keyboardHidden|orientation"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/NoActionBar" >
        <activity
            android:name="com.example.controlefantasias.MainScreenActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All Product Activity -->
        <activity
            android:name="com.example.controlefantasias.AllProductsActivity"
            android:label="Fantasias" >
        </activity>

        <!-- Add Product Activity -->
        <activity
            android:name="com.example.controlefantasias.NewProductActivity"
            android:label="Add New Product" >
        </activity>

        <!-- Edit Product Activity -->
        <activity
            android:name="com.example.controlefantasias.EditProductActivity"
            android:label="Edit Product" >
        </activity>

        <!-- All Client Activity -->
        <activity
            android:name="com.example.controlefantasias.AllClientsActivity"
            android:label="Clientes" >
        </activity>

        <!-- Add Client Activity -->
        <activity
            android:name="com.example.controlefantasias.NewClientActivity"
            android:label="Add New Client" >
        </activity>

        <!-- Edit Client Activity -->
        <activity
            android:name="com.example.controlefantasias.EditClientActivity"
            android:label="Edit Client" >
        </activity>
    </application>

</manifest>